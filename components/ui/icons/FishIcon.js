import Icon from "./Icon";

export default FishIcon = () => {
  const iconProps = {
    color: "#FFA700",
    name: "phishing",
  };

  return <Icon color={iconProps.color} name={iconProps.name} />;
};
